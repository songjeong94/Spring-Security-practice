spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/userjoinlogin?serverTimezone=UTC&characterEncoding=UTF-8
    username: root
    password: thdwjd
  jpa:
    database: mysql
    database-platform: org.hibernate.dialect.MySQL8Dialect
    show-sql: false
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: true


logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type: trace


jwt:
  secret-key: exhibition-sns-project-2023.secret_key
  # 30일
  token.expired-time-ms: 2592000000
